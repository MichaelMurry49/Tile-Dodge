// const cvs = document.getElementById('start-menu');
// const ctx = cvs.getContext('2d');

// const tile = 30;
// ctx.fillStyle = "yellow";
// ctx.fillRect(tile * 1, tile * 4, tile, tile);

// function draw() {
    const cvs = document.getElementById('start-menu');
    const ctx = cvs.getContext('2d');

    const tile = 30;
    ctx.fillStyle = "yellow";
    ctx.fillRect(tile * 1, tile * 4, tile, tile);
    
    // var ctx = document.getElementById('start-menu').getContext('2d');
    // ctx.font = '48px serif';
    // ctx.fillText('Hello world', 10, 50);
// }
